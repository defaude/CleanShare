{
  "name": "web-demo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "PATH=$PATH:$HOME/.cargo/bin WASM_PACK_CACHE=../../.wasm-pack-cache wasm-pack build ../../crates/link_cleaner_wasm --target web --out-dir ../../apps/web_demo/pkg --out-name link_cleaner_wasm",
    "dev": "npm run build:wasm && vite --port 5173",
    "build": "npm run build:wasm && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
